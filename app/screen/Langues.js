import React from 'react';
import ItemDrawer from '../views/ItemDrawer';
import { View, Text, StyleSheet } from 'react-native';

const Langues = ({ navigation }) => {
    return (
            <ItemDrawer navigation={navigation} name="Langues" />

    );
}
export {Langues};