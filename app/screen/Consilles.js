import React from 'react';
import ItemDrawer from '../views/ItemDrawer';
import { View, Text, StyleSheet } from 'react-native';

const Consilles = ({ navigation }) => {
    return (

            <ItemDrawer navigation={navigation} name="Consilles" />


    );
}
export {Consilles};