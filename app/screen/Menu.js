import React from 'react';
import ItemDrawer from '../views/ItemDrawer';
import { View, Text, StyleSheet } from 'react-native';

export const Menu = ({ navigation }) =>
    <ItemDrawer navigation={navigation} name='Menu' />
